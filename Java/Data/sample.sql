CREATE DATABASE SAMPLE_DATA;
CREATE TABLE SAMPLE_INFO (
	--Numbers are expected to change based on the data
	UNIQ_ID INT AUTO_INCREMENT,
	LDATE VARCHAR(40),
	MD5HASH VARCHAR(32),
	UTCTIME VARCHAR(30),
	
)